# sitecustomize.py  (autoloaded by Python if present on sys.path)
import os, sys
root = os.path.dirname(__file__)
if root not in sys.path:
    sys.path.insert(0, root)  # ensure repo root comes before site-packages
