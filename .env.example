cp .env .env.example
# open and replace creds with placeholders like:
# SNOWFLAKE_USER=your_username
# SNOWFLAKE_PASSWORD=your_password
git add .env.example
git commit -m "Add .env.example for safe environment variable template"
git push
